{
  "name": "express-rest-api-no-db",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "node_modules/.bin/nodemon --harmony -e js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "npx prettier --write helpers/**/**.js && npx prettier --write model/**/**.js && npx prettier --write routes/**/**.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "morgan": "^1.9.1",
    "nodemon": "^1.18.10",
    "prettier": "^1.16.4"
  }
}
